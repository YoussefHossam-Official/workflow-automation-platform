import React from 'react';

export default function App(){
  return (
    <div style={{ padding: 20, fontFamily: 'Arial, sans-serif' }}>
      <h1>Workflow Automation — Client</h1>
      <p>This is a minimal client skeleton. Use it to build a dashboard that talks to the API.</p>
    </div>
  );
}
